# Dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Next.js
/.next/
/out/
/build

# Production / caches
.turbo
.vercel
.eslintcache
*.tsbuildinfo

# Testing
/coverage

# Environment & secrets (never commit real keys)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env*.local
# Allow the committed template
!.env.example

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# OS
.DS_Store
Thumbs.db
Desktop.ini

# Editor / IDE (local preferences)
.idea/
*.swp
*.swo
*~

# Cursor local agent state; keep shared rules committable
.cursor/*
!.cursor/rules/
!.cursor/rules/**

# Certificates / keys
*.pem
*.key
*.p12

# Optional: local Playwright / agent artifacts (Phase 4+)
/playwright-report/
/test-results/
/blob-report/

/Plan.md
/CLAUDE.md